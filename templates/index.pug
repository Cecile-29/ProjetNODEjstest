doctype html
html(lang="fr")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
        h1 Bienvenue dans le formulaulaire"ire
        button  #{invit}
            
        

    script(src=process.env.BROWSER_REFRESH_URL)